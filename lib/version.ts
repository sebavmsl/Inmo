/**
 * Versión del sistema v2. A partir de V.201 (primer deploy que compiló y
 * corrió limpio en Vercel) se numeran las entregas correlativamente.
 * V.202 = fixes de esquema real (honorarios, act_contrato, inicio_contrato,
 * garantia_pagada, contratos sin columna id) tras correr las 12 migraciones
 * contra producción. Se muestra como sello fijo en el Sidebar y en el pie
 * del login.
 */
export const APP_VERSION = "V.202";
